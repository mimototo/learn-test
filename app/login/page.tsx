import { LoginForm } from "@/feature/login/components/LoginForm";

const page = () => {
  return (
    <main className="max-w-md flex mx-auto py-24">
      <LoginForm />
    </main>
  );
};

export default page;
